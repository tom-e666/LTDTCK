#include <iostream>
#include <string>

int main()
{

    // Read input using std::getline
    std::string input;
    std::getline(std::cin, input);

    // Process the input (replace this with your actual logic)
    std::string output = "C++ program processed: " + input;

    // Print the output to the standard output
    std::cout << output << std::endl;

    return 0;
}
